namespace account;

/// Status account's profile status define
enum Status:byte {
    None  =0,
    WaitForEmailVerify,
    Active,
    Suspend,
    Deleted,
  }
/// Role account's access role define
enum Role:byte {
  Guest = 0,
    Member ,
    VIP ,
}
