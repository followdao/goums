syntax = "proto3";

package account;

// Status account's status define
enum Status {
    None = 0;
    WaitForEmailVerify = 1;
    Active = 2;
    Suspend = 4;
    Deleted = 8;
}

// Role account's rols define
enum Role {
    Guest = 0;
    Member = 1;
    VIP = 2;
}

